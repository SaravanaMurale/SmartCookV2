<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.clj.blesample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/preethi_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".menuoperationactivity.ProfileSettingsActivity"></activity>
        <!-- <activity android:name=".operation.BarChartActivity"></activity> -->
        <!-- <activity android:name=".operation.MenuActivity" /> -->
        <!-- <activity android:name=".MainActivity"></activity> -->
        <!-- <activity android:name=".DummyActivity"></activity> -->
        <activity android:name=".menuoperationactivity.MenuActivity" />
        <activity android:name=".DummyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".operation.OperationActivity"
            android:screenOrientation="landscape" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>